{{ if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  labels:
    app: kafka
  name: {{ .Values.serviceName }}
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - port: 9092
      name: client
      protocol: TCP
      targetPort: 9092
    - port: 9093
      name: controller
      protocol: TCP
      targetPort: 9093
  selector:
    app: {{ .Values.serviceName }}
  type: ClusterIP
{{ end }}
